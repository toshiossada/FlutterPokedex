import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:pokedex/app/modules/poke_detail/pages/about/about_page.dart';

main() {
  testWidgets('AboutPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AboutPage(title: 'About')));
    //  final titleFinder = find.text('About');
    //  expect(titleFinder, findsOneWidget);
  });
}
